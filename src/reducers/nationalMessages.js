// example data format 
const initialState = {
};


export default function nationalMessagesReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};