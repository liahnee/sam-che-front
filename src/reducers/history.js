// example data format 
// 연감, 명예의전당, 왕조, etc 
const initialState = {
};


export default function historyReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};