// example data format 
// includes all cities, documents, policies, commands, userlists, troops, npc정책, etc
const initialState = {
};


export default function allNationalDataReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};