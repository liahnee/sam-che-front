// example data format 
// 접속량, liver user, live countries, etc

const initialState = {
};


export default function serverDataReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};