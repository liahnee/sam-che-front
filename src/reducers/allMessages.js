// example data format 
const initialState = {
};


export default function allMessagesReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};