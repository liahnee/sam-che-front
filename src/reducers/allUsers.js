// example data format 
const initialState = {
};


export default function allUsersReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};