// example data format 
const initialState = {
};


export default function allNationsReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};