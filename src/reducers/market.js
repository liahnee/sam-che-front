// example data format 
const initialState = {
};


export default function marketReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};