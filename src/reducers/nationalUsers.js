// example data format 
const initialState = {
};


export default function nationalUsersReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};