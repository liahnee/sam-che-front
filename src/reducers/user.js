// example data format 
// 내정보, 개인기록, etc

const initialState = {
};


export default function userReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};