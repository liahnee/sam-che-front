// example data format 
const initialState = {
};


export default function userMessagesReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};