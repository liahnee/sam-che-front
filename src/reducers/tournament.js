// example data format 
// tournament and bettingGround
const initialState = {
};


export default function tournamentReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};