// example data format 
const initialState = {
};


export default function diplomaticMessagesReducer(state = initalState, action) {
    switch(action.type) {

        default:
            return state; 
    };
};