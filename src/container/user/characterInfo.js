import React from 'react';
import { connect } from "react-redux";

 const CharacterInfo = props => {



    return (
        <div className=''> 
        </div>
    );
};

const sToP = state => {
    return {

    };
}

const dToP = dispatch => {
    return {

    };
}

export default connect(sToP, dToP)(CharacterInfo);
