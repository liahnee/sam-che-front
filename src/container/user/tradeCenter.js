import React from 'react';
import { connect } from "react-redux";

 const TradeCenter = props => {



    return (
        <div className=''> 
        </div>
    );
};

const sToP = state => {
    return {

    };
}

const dToP = dispatch => {
    return {

    };
}

export default connect(sToP, dToP)(TradeCenter);
